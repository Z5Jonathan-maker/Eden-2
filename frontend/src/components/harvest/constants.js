/**
 * Harvest Module Constants — DoorMamba-class 6-pin system
 * Re-exported from features/harvest for backward compat
 */
export {
  STATUSES,
  STATUS_BY_CODE,
  FIELD_MODE_PINS,
  PIN_STATE_MACHINE,
  DEFAULT_CENTER,
  DEFAULT_ZOOM,
  FIELD_MODE_ZOOM,
  RARITY_COLORS,
} from '../../features/harvest/components/constants';
