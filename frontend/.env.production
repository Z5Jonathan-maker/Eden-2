# Empty = use Vercel proxy (same-origin /api/* rewrites to backend)
# Vercel rewrites in vercel.json forward all headers including Authorization
REACT_APP_BACKEND_URL=
REACT_APP_ASSET_BASE_URL=/assets/contracts
REACT_APP_IMAGE_BASE_URL=/images
REACT_APP_CONFIG_VERSION=2026-PROD
REACT_APP_ENABLE_OFFLINE_QUEUE=true
